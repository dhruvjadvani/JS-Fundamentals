/*
In the code below create an array named threeArray with five positions and fill all the positions with the number 3 using the fill() method.

In the end just print the array.
*/

let threeArray = new Array(5).fill(3);
console.log(threeArray);
